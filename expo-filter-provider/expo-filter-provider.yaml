apiVersion: v1
kind: Service
metadata:
  name: expo-filter-provider
  namespace: experience-tools
  labels:
    app: expo-filter-provider
    service: expo-filter-provider
    version: v1
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: expo-filter-provider
    version: v1
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: expo-filter-provider-v1
  namespace: experience-tools
  labels:
    app: expo-filter-provider
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expo-filter-provider
      version: v1
  template:
    metadata:
      labels:
        app: expo-filter-provider
        version: v1
    spec:
      containers:
      - name: expo-filter-provider
        image: "docker.prod.walmart.com/experience-tools/expo-filter-provider:0.0.11"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: expo-filter-provider-gateway
  namespace: experience-tools
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: expo-filter-provider-vitual-service
  namespace: experience-tools
spec:
  hosts:
  - "*"
  gateways:
  - expo-filter-provider-gateway
  http:
  - match:
    - uri:
        prefix: /filter
    route:
    - destination:
        host: expo-filter-provider
        port:
          number: 8080